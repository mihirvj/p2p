typedef struct hostnode
{
  
  char host[16];
  int port;	
  struct hostnode *next;
}hnode;

void add_hnode(hnode **head, int key, int data);
//int update_node(node *head, int key, int new_data);
void traverse(node *head);
//node** lookupall(node *head, int key);
void destroy_list(node **head);
int delete_hnode(node **head, int key);
